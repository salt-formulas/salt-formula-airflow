{%- from "stories/map.jinja" import server with context %}
# Fill in documentation details
doc:
  name: stories
  description: Some service info
  role:
    server:
      name: server
      param:
        some_param:
          name: "Some name"
          value: "some value"
